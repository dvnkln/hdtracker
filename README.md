# hdtracker

Self-hosted Media-Tracker für Filme, Serien, Anime und Spiele.

## Start mit Docker

```bash
cp .env.example .env   # Secrets eintragen
docker compose up -d
```

App: http://localhost:3000 · Health: http://localhost:3000/health

## Entwicklung

```bash
npm install
npm run dev
```
